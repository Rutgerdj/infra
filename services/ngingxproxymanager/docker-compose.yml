services:
  app:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - '80:80'
      - '81:81'
      - '443:443'
    volumes:
      - "{{ service_config_path }}/data:/data"
      - "{{ service_config_path }}/letsencrypt:/etc/letsencrypt"

services:
  renovate:
    image: renovate/renovate:42.19.3
    container_name: renovate
    restart: no
    environment:
      - RENOVATE_TOKEN={{docker_stacks.renovate.gh_token}}
    volumes:
      - ./config.js:/usr/src/app/config.js:ro

services:
  metube:
    image: ghcr.io/alexta69/metube:2024-08-22
    container_name: metube
    restart: unless-stopped
    ports:
      - "{{ docker_stacks.metube.port }}:8081"
    volumes:
      - "{{ service_config_path }}:/downloads"
    environment:
      - DELETE_FILE_ON_TRASHCAN=true
